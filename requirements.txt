# --- Backend (Phase 1 foundation) ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0

# --- Auth / Email (Phase 2) ---
itsdangerous==2.2.0
email-validator==2.2.0
httpx==0.28.1

# --- Frontend ---
streamlit==1.41.1

# --- Scheduler (Phase 6) ---
APScheduler==3.10.4

# --- Gemini AI (Phase 7) ---
# Uses the Gemini REST API via httpx (already required) — no extra SDK needed.
